{
  "name": "clipbrain",
  "version": "0.1.0",
  "description": "Local memory layer for AI agents via ClipBrain",
  "family": "bundle-plugin",
  "mcpServers": {
    "gbrain": {
      "command": "gbrain",
      "args": ["serve"]
    }
  },
  "openclaw": {
    "compat": {
      "pluginApi": ">=2026.4.0"
    }
  }
}
